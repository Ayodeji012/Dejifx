<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DejiFX - Advanced Forex Trading</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        /* ===== CSS Variables ===== */
        :root {
            --primary: #003366;
            --primary-dark: #002244;
            --secondary: #4CAF50;
            --secondary-dark: #2E7D32;
            --accent: #5865F2;
            --accent-dark: #4752C4;
            --text: #ffffff;
            --text-secondary: #b0b0b0;
            --background: #0a0e17;
            --card-bg: rgba(19, 26, 42, 0.8);
            --glow: 0 0 20px rgba(76, 175, 80, 0.3);
        }

        /* ===== Base Styles ===== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            color: var(--text);
            background: var(--background);
            overflow-x: hidden;
            line-height: 1.6;
            perspective: 1000px;
        }

        /* ===== 3D Background System ===== */
        .cosmic-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
            transform-style: preserve-3d;
        }

        /* Cosmic Nebula Background */
        .cosmic-bg {
            position: absolute;
            width: 200%;
            height: 200%;
            top: -50%;
            left: -50%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 51, 102, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(76, 175, 80, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(88, 101, 242, 0.15) 0%, transparent 50%),
                linear-gradient(135deg, #0a0e17 0%, #131a2a 100%);
            animation: cosmic-drift 120s infinite linear;
            transform: translateZ(-500px) scale(2);
        }

        /* 3D Particles System */
        .particles {
            position: absolute;
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
        }

        .particle {
            position: absolute;
            background: rgba(255, 255, 255, 0.6);
            border-radius: 50%;
            animation: float 20s infinite ease-in-out;
            transform-style: preserve-3d;
        }

        /* Create multiple particles with different sizes and animations */
        .particle:nth-child(1) { width: 2px; height: 2px; top: 20%; left: 10%; animation-duration: 25s; }
        .particle:nth-child(2) { width: 1px; height: 1px; top: 60%; left: 80%; animation-duration: 30s; }
        .particle:nth-child(3) { width: 3px; height: 3px; top: 40%; left: 50%; animation-duration: 20s; }
        .particle:nth-child(4) { width: 2px; height: 2px; top: 80%; left: 30%; animation-duration: 35s; }
        .particle:nth-child(5) { width: 1px; height: 1px; top: 30%; left: 70%; animation-duration: 28s; }
        .particle:nth-child(6) { width: 2px; height: 2px; top: 50%; left: 20%; animation-duration: 32s; }

        /* 3D Trading Charts */
        .chart-3d {
            position: absolute;
            width: 300px;
            height: 150px;
            background: linear-gradient(45deg, rgba(0, 51, 102, 0.3), rgba(76, 175, 80, 0.3));
            border: 1px solid rgba(76, 175, 80, 0.2);
            transform-style: preserve-3d;
            backdrop-filter: blur(5px);
            border-radius: 8px;
            overflow: hidden;
        }

        .chart-3d::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                linear-gradient(90deg, transparent 95%, rgba(76, 175, 80, 0.1) 100%),
                repeating-linear-gradient(0deg, rgba(76, 175, 80, 0.05) 0px, rgba(76, 175, 80, 0.05) 1px, transparent 1px, transparent 20px);
            animation: chart-glow 3s infinite alternate;
        }

        .chart-3d:nth-child(1) {
            top: 15%;
            left: 5%;
            transform: translateZ(-200px) rotateY(15deg) rotateX(10deg);
            animation: chart-float-1 25s infinite ease-in-out;
        }

        .chart-3d:nth-child(2) {
            top: 65%;
            right: 5%;
            transform: translateZ(-300px) rotateY(-15deg) rotateX(-5deg);
            animation: chart-float-2 30s infinite ease-in-out;
        }

        /* 3D Grid System */
        .grid-3d {
            position: absolute;
            width: 200%;
            height: 200%;
            top: -50%;
            left: -50%;
            background-image: 
                linear-gradient(rgba(76, 175, 80, 0.1) 1px, transparent 1px),
                linear-gradient(90deg, rgba(76, 175, 80, 0.1) 1px, transparent 1px);
            background-size: 50px 50px;
            transform: translateZ(-400px) rotateX(75deg);
            animation: grid-move 20s infinite linear;
            perspective: 500px;
        }

        /* ===== Content Styles ===== */
        .content {
            position: relative;
            z-index: 10;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Styles */
        header {
            background: rgba(19, 26, 42, 0.8);
            backdrop-filter: blur(10px);
            padding: 20px 0;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(76, 175, 80, 0.2);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--secondary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: var(--glow);
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 30px;
        }

        nav a {
            color: var(--text);
            text-decoration: none;
            font-weight: 500;
            padding: 10px 15px;
            border-radius: 5px;
            transition: all 0.3s ease;
            position: relative;
        }

        nav a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 2px;
            background: var(--secondary);
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        nav a:hover::before {
            width: 80%;
        }

        nav a:hover {
            color: var(--secondary);
        }

        /* Hero Section */
        .hero {
            padding: 180px 0 100px;
            text-align: center;
            position: relative;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 20px;
            background: linear-gradient(45deg, var(--text), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 30px rgba(76, 175, 80, 0.5);
        }

        .hero p {
            font-size: 1.3rem;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto 40px;
        }

        /* Trading Cards */
        .trading-cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin: 60px 0;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid rgba(76, 175, 80, 0.1);
            border-radius: 15px;
            padding: 30px;
            backdrop-filter: blur(10px);
            transform-style: preserve-3d;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, var(--secondary), transparent);
        }

        .card:hover {
            transform: translateY(-10px) rotateX(5deg);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            border-color: rgba(76, 175, 80, 0.3);
        }

        .card h3 {
            font-size: 1.5rem;
            margin-bottom: 15px;
            color: var(--secondary);
        }

        /* Buttons */
        .btn {
            display: inline-block;
            padding: 15px 35px;
            background: linear-gradient(45deg, var(--secondary), var(--secondary-dark));
            color: white;
            text-decoration: none;
            border-radius: 30px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: all 0.5s ease;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(76, 175, 80, 0.3);
        }

        .btn-discord {
            background: linear-gradient(45deg, var(--accent), var(--accent-dark));
        }

        /* ===== Animations ===== */
        @keyframes cosmic-drift {
            0% { transform: translateZ(-500px) scale(2) rotate(0deg); }
            100% { transform: translateZ(-500px) scale(2) rotate(360deg); }
        }

        @keyframes float {
            0%, 100% { transform: translate3d(0, 0, 0) rotate(0deg); }
            50% { transform: translate3d(100px, 50px, 50px) rotate(180deg); }
        }

        @keyframes chart-float-1 {
            0%, 100% { transform: translateZ(-200px) rotateY(15deg) rotateX(10deg) translateY(0); }
            50% { transform: translateZ(-200px) rotateY(25deg) rotateX(15deg) translateY(-20px); }
        }

        @keyframes chart-float-2 {
            0%, 100% { transform: translateZ(-300px) rotateY(-15deg) rotateX(-5deg) translateY(0); }
            50% { transform: translateZ(-300px) rotateY(-25deg) rotateX(-10deg) translateY(20px); }
        }

        @keyframes grid-move {
            0% { transform: translateZ(-400px) rotateX(75deg) translateY(0); }
            100% { transform: translateZ(-400px) rotateX(75deg) translateY(50px); }
        }

        @keyframes chart-glow {
            0% { box-shadow: 0 0 20px rgba(76, 175, 80, 0.1); }
            100% { box-shadow: 0 0 40px rgba(76, 175, 80, 0.3); }
        }

        /* ===== Responsive Design ===== */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            .hero p { font-size: 1.1rem; }
            nav ul { gap: 15px; }
            .trading-cards { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>
    <!-- 3D Background System -->
    <div class="cosmic-container">
        <div class="cosmic-bg"></div>
        <div class="grid-3d"></div>
        <div class="particles">
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
        </div>
        <div class="chart-3d"></div>
        <div class="chart-3d"></div>
    </div>

    <!-- Content -->
    <div class="content">
        <header>
            <div class="container header-content">
                <div class="logo">DejiFX</div>
                <nav>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#about">About</a></li>
                        <li><a href="#strategies">Strategies</a></li>
                        <li><a href="https://discord.gg/xGXxxAzf" class="btn btn-discord">Join Discord</a></li>
                    </ul>
                </nav>
            </div>
        </header>

        <section id="home" class="hero">
            <div class="container">
                <h1>Master Forex Trading in 3D</h1>
                <p>Experience the future of trading education with our immersive 3D platform. Learn advanced strategies, connect with experts, and elevate your trading skills.</p>
                <a href="#strategies" class="btn">Explore Strategies</a>
            </div>
        </section>

        <section class="container">
            <div class="trading-cards">
                <div class="card">
                    <h3>Real-time Analysis</h3>
                    <p>Get live market analysis and trading signals from our expert team. Stay ahead of market movements with our advanced analytics.</p>
                </div>
                <div class="card">
                    <h3>3D Charting</h3>
                    <p>Experience market data like never before with our immersive 3D charting technology. Visualize trends and patterns in stunning detail.</p>
                </div>
                <div class="card">
                    <h3>Community Support</h3>
                    <p>Join our active Discord community of traders. Share insights, ask questions, and grow together with like-minded individuals.</p>
                </div>
            </div>
        </section>

        <section id="strategies" class="container" style="padding: 100px 0;">
            <h2 style="text-align: center; font-size: 2.5rem; margin-bottom: 50px; color: var(--secondary);">Trading Strategies</h2>
            <div class="trading-cards">
                <div class="card">
                    <h3>Day Trading</h3>
                    <p>Master short-term price movements with our comprehensive day trading strategies. Perfect for active traders seeking daily opportunities.</p>
                </div>
                <div class="card">
                    <h3>Swing Trading</h3>
                    <p>Capture medium-term market swings with our proven swing trading techniques. Ideal for traders with limited screen time.</p>
                </div>
                <div class="card">
                    <h3>Position Trading</h3>
                    <p>Long-term strategies based on fundamental analysis. Learn to identify major trends and position yourself for significant gains.</p>
                </div>
            </div>
        </section>
    </div>

    <script>
        // Enhanced 3D effects with mouse interaction
        document.addEventListener('mousemove', function(e) {
            const x = (window.innerWidth / 2 - e.clientX) / 25;
            const y = (window.innerHeight / 2 - e.clientY) / 25;
            
            document.querySelector('.cosmic-bg').style.transform = 
                `translateZ(-500px) scale(2) rotate(${x}deg) rotate3d(1, 0, 0, ${y}deg)`;
            
            document.querySelector('.grid-3d').style.transform = 
                `translateZ(-400px) rotateX(75deg) translateY(${y * 2}px) translateX(${x * 2}px)`;
        });

        // Parallax effect for cards
        const cards = document.querySelectorAll('.card');
        cards.forEach(card => {
            card.addEventListener('mousemove', function(e) {
                const x = e.offsetX;
                const y = e.offsetY;
                const rotateY = (-1/5 * x + 20);
                const rotateX = (4/30 * y - 20);
                
                this.style.transform = `perspective(1000px) rotateX(${rotateX}deg) rotateY(${rotateY}deg) translateZ(10px)`;
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'perspective(1000px) rotateX(0) rotateY(0)';
            });
        });

        // Add more particles dynamically
        function createParticles() {
            const particlesContainer = document.querySelector('.particles');
            for (let i = 0; i < 20; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.width = Math.random() * 3 + 1 + 'px';
                particle.style.height = particle.style.width;
                particle.style.top = Math.random() * 100 + '%';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDuration = Math.random() * 30 + 15 + 's';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particlesContainer.appendChild(particle);
            }
        }
        
        createParticles();
    </script>
</body>
</html>
